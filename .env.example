# Vite only exposes VITE_* variables, and it inlines them into the client bundle.
# Nothing here is a secret. Server-only secrets must never use the VITE_ prefix.

VITE_API_URL=http://localhost:3001/api

# Serve the MSW contract instead of a real backend during development.
VITE_ENABLE_MSW=true
